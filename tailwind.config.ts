// tailwind.config.ts
const config = {
  theme: {
    extend: {
      colors: {
        // Tesla-inspired palette
        background: "#0a0a0a", // Deep Onyx (Better for text visibility)
        surface: "#141414",    // Lighter gray for cards
        primary: "#00FF00",    // Neon Green (Your Branding)
        textMain: "#ffffff",   // Pure White
        textMuted: "#a1a1a1",  // High-visibility gray
      },
    },
  },
};